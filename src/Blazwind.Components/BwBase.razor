@inherits Microsoft.AspNetCore.Components.ComponentBase

@* BwBase - Base component for Blazwind components *@
@* Provides common parameters: Class, Style, Id, AdditionalAttributes *@

@code {
    /// <summary>
    /// Additional CSS classes to apply to the component.
    /// </summary>
    [Parameter]
    public string? Class { get; set; }

    /// <summary>
    /// Inline styles to apply to the component.
    /// </summary>
    [Parameter]
    public string? Style { get; set; }

    /// <summary>
    /// The HTML id attribute.
    /// </summary>
    [Parameter]
    public string? Id { get; set; }

    /// <summary>
    /// Additional attributes to apply to the root element.
    /// </summary>
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }

    /// <summary>
    /// Combines the base CSS classes with user-provided classes.
    /// </summary>
    protected string CombineClasses(params string?[] classes)
    {
        var builder = new System.Text.StringBuilder();
        
        foreach (var c in classes)
        {
            if (!string.IsNullOrWhiteSpace(c))
            {
                if (builder.Length > 0) builder.Append(' ');
                builder.Append(c);
            }
        }

        if (!string.IsNullOrWhiteSpace(Class))
        {
            if (builder.Length > 0) builder.Append(' ');
            builder.Append(Class);
        }

        return builder.ToString();
    }
}
